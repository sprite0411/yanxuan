export default {
  receive_tabNav (state,tabNav) {
    state.tabNav = tabNav
  },
  receive_tabContent(state,tabContent){
    state.tabContent=tabContent
  },
  receive_auto(state,autoData){
    state.autoData=autoData
  }
}
